model Token {
  userId                    String          @unique
  lastUsed                  DateTime        
  token                     String          @default("")
  tokenHasBeenRemoved       Boolean         @default(false)
}